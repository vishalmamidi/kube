# get cluster info

kubectl cluster-info

kubectl get all



## helm

to see final deployemnt and service files 

  ```
  helm template <name>
  ```
  
 to verify chats 
 
   ```
  helm lint <name>
  ```
